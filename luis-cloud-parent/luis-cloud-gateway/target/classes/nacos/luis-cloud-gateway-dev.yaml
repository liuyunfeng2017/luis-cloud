spring:
  cloud:      
    gateway: 
      enabled: true 
      discovery:
        locator:
          lower-case-service-id: true
      routes:  
        - id: luis_user
          predicates:
            - Path=/user/**
          uri: lb://luis-cloud-user
          filters:
            - StripPrefix=1

  redis:
    host: 120.79.150.108
    port: 6379
    password: passwd123
    database: 10
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: 10000
      shutdown-timeout: 100

